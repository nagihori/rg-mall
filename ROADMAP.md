# Game Event CMS Roadmap

## v1

- Next.js の公開サイトと Payload の管理画面を同一プロジェクトで構築する。
- イベント、メディア、ユーザー、監査ログのデータモデルと権限を実装する。
- 下書き、確認依頼、公開、アーカイブの運用フローを実装する。
- テキスト、メイン写真、ギャラリー写真、開催日時を入力できるようにする。
- 開催中／予定／新着／アーカイブの一覧とイベント詳細を実装する。
- 日本語の入力補助、プレビュー、明瞭なエラーメッセージを用意する。
- バックアップ、メディア保存、公開反映、監査ログの運用を設定する。
- Discord OAuth ログインと、確認依頼・公開結果の Discord 通知を実装する。
- 無料枠の利用量確認、上限到達時の停止・縮退手順を整備する。

## 次段階

- 予約公開と公開期限の指定
- 年・タグ・ゲーム内カテゴリによるアーカイブ検索
- 多言語コンテンツと翻訳のレビュー
- ゲーム内ニュースや Discord 等への配信連携
- 画像のトリミング補助、テンプレート、アクセシビリティチェック
- 更新担当者の操作状況に合わせた管理画面の簡略化

## 今はやらないこと

- 自由なページレイアウトを作るページビルダー
- プレイヤー向けのログイン、コメント、投稿
- イベント参加予約や決済
- 複雑な分析ダッシュボード
- 自動生成された本文を無確認で公開する機能

## 管理画面まわりの積み残し（編集画面MVP完了時点）

- イベント一覧画面の作り込み（削除用チェックボックス、サムネイル、ステータス、公開/プレビューリンク、最終更新日時・ユーザー、確認依頼日時・ユーザー、ロール別アクション、編集者は自分の記事がデフォルト、確認者・管理者は未承認タスクと自分の記事を別表示、等）。今回はスコープ外として全面ステイ。
- 確認者・管理者が下書きから直接公開できる「レビュー飛ばし」機能。当面は運用でカバーし、実装は見送り。
- 「過去のイベント」状態でも本文フィールドを編集ロックする（現状は「トップページに表示中」のときのみロック。過去のイベントは直接編集するとレビューを経ずに内容が変わってしまう）。
- 画像選択をサムネイル表示にする（メイン画像・ギャラリー画像のrelationshipをupload型に変更、メディア一覧にもサムネイル列を追加）。加えて既定で自分がアップロードした画像に絞り込む。relationship→upload はスキーマ変更・マイグレーションを伴うため中規模タスク。
- イベント複製機能。`sourceEvent` フィールドはスキーマに存在するが、複製ボタン・複製フックとも未実装で使われていない。「公開中の記事を複製して下書きを作り、公開時に元記事を差し替える」という運用フロー自体を先に設計する必要がある。

## 実装の順序

1. 実運用の告知 5〜10 件を使って項目と一覧の試作を確認する。
2. 認証・権限・状態遷移・監査ログを先に固める。
3. 入力画面と公開画面を実装し、更新担当者による操作テストを行う。
4. 写真アップロード、画像最適化、プレビュー、公開反映を接続する。
5. 本番移行前にバックアップ復元と誤公開の取消手順を演習する。
6. Discord ログイン、確認依頼通知、無料枠の上限到達時の挙動を検証する。
